~~~
git clone https://github.com/tingkai-c/.tmux.git ~/.tmux && cp ~/.tmux/tmux.conf ~/.tmux.conf && mkdir -p ~/.tmux/plugins/tpm && git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins
~~~
