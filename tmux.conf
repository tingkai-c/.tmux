set-option -sa terminal-overrides ",xterm*:Tc"
set -g mouse on

set -g allow-passthrough on
set -g xterm-keys on
set -g history-limit 100000

# Do not keep orphaned interactive sessions after the last client detaches.
# This prevents `tmux ls` from accumulating sessions that are no longer visible.
set -g destroy-unattached on

#unbind C-b
#set -g prefix C-Space
#bind C-Space send-prefix

# Vim style pane selection
bind h select-pane -L
bind j select-pane -D 
bind k select-pane -U
bind l select-pane -R

# C-b r for renaming session
unbind $
bind r command-prompt -I "#S" "rename-session '%%'"


# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Use Alt-arrow keys without prefix key to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window


set -g @rose_pine_variant 'main'
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'rose-pine/tmux'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'omerxx/tmux-sessionx'
run '~/.tmux/plugins/tpm/tpm'

# set vi-mode
set-window-option -g mode-keys vi
bind-key v copy-mode
# keybindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel
# bind-key ] run "reattach-to-user-namespace pbpaste | tmux load-buffer -" \; paste-buffer

unbind '"'
unbind %
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Pane menu: C-b p
unbind p
bind p run-shell -b "$HOME/.config/tmux/pane-menu.sh \"#{pane_id}\" 0"
